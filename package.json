{
  "name": "truecost",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:styles": "node tooling/build-styles.mjs",
    "build": "node build.mjs",
    "test:unit": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "html-minifier-terser": "^7.2.0"
  }
}
